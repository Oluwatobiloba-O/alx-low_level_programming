#include "main.h"
/* betty style doc for function main goes there */
/**
 * _abs - Entry point (_abs)
 * @x: _abs
 * Return: Always 0
 */
int _abs(int x)
{
if (x < 0)
return (-x);
else if (x >= 0)
{
return (x);
}
return (0);
}
