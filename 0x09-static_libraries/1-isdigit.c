#include "main.h"
/* betty style doc for function main goes there */
/**
 * _isdigit - Entry point (_isdigit)
 *
 * @c: _isdigit
 * Return: Number
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}

return (0);
}
