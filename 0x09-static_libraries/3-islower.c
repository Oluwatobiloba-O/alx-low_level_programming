#include "main.h"
/* betty style doc for function main goes there */
/**
 * _islower - Entry point (islower)
 *
 * @c: list count
 * Return: Return 0 or 1
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
